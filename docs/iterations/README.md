# Future Iterations

This directory tracks possible work after the initial release. The [post-MVP
roadmap](roadmap.md) orders the current candidates by dependency and learning value.

Roadmap entries are not release commitments and do not define approved APIs. Before a
candidate becomes an implementation iteration, its user problem, design, guarantees,
backend requirements, migration risks, and delivery scope must be evaluated.
